核查完成。这一版我把新仓库全部 41 个 .py 逐一读过（含 4.9 万行的 `submission_fast.py` 新链路）、4 张表逐格核数、9 张图经 OCR + 渲染代码双向核对，并针对三个最容易藏 bug 的新机制写了数值模拟验证。先给总判定，再给批评与建议。

## 一、总判定：口径已修正，核心方法链无系统性偏误，无致命缺陷

**两个面板定义现在与你的口径严格一致，且标签诚实。** Figure 1 = 逐年 `is_strict_{year}`（年内全勤、跨年池变，N=1319/988/557/…/4635）；Figure 2 = 13 年固定交集（N 恒 115），逐年只做切片。Table I/II 的两个 panel_block 直接以 Figure 1/2 语义命名并附 `sample_semantics` 列。上一版的三个 P0 全部消失：逐年选样不再错列、`valid_days>0` 松宇宙已从交稿链路移除、pairwise PCA 仅剩 NaN 兜底（strict 面板无 NaN，永不触发）。

**核心估计链逐项复核通过**：标准化→(1/N)R′R→Λ=√N·V→F=RsΛ/N、TOD 阈值跳跃、g=√N·median、对称白化 GC、符号定向、切点组合 SR=√(μ′Σ⁻¹μ)、时间序列定价（α=截距、预测=β·mean(F)）全部正确。架构上最大的进步是：**核心 PCA 每次运行现场重算**（115 股面板足够快），不再依赖 33 小时旧 pickle——上一版"跨代码版本混跑"的整类风险被结构性消灭了。

**三个新机制的模拟验证全过**：(1) 逐年 Procrustes 双旋转（q1@q2 链、SVD 解、旋转序）在"真同因子空间"DGP 下恢复 GC≈[0.999,0.998,0.995,0.990]——所以 Table III Panel C 实测的 {0.86,0.73,0.60,0.41} 是真实的经济信号（幸存者敏感度），不是构造伪影；(2) Table V 从 Figure 13 归一化累计收益反推收益的做法，因"同因子三段共用同一日频 std"而对单因子 SR、同段切点 SR、跨段加权 SR **精确不变**（模拟差异 7e-9，纯求解器噪声）；(3) rf 处理已按 footnote 33 修正——PCA/对齐 PCA 行不减 rf，FFC 用 4/24–20/24，上一版"隔夜负溢价被 rf 系统性夸大"的头号偏误已除。2019 年 GC₂=0.008 这类怪值我也模拟复现了：是特征值接近时 K=2 截断的排序交换效应（K=3 即恢复 0.65），属解读事项而非 bug。

## 二、批评：四个需要处理的系统性风险

**1. N=115 是全仓库最大的单点风险，且恰恰不是论文自己的口径。** 你的定义 A 措辞（"都连续存在、都能用"）支持 strict=115，这个选择站得住；但要清醒：论文附录 A 本身用的是宽松过滤+零插值（对应你们的 paper_lenient=391），115/5465≈2% 是比论文 332/600≈55% 严苛一个量级的幸存者切片（13 年零停牌 ≈ 大市值/高流动性选择）。所有下游产物（Fig 4/7/10/13/14/15、Table III/V 基准）都建立在这 115 只上。Table II 的 GC₁=0.90–0.99 为市场因子代表性提供了辩护，但 GC₂ 在 0.007–0.90 之间摆、GC₃ 更弱——**第 2 因子以后的代表性只有部分成立**。建议：正文保留 115（贴你的定义），但用已写好的 paper_lenient 面板（loader 现已放行、预处理已产出 391 面板）跑**一次** Table III/V 的 391 稳健列；caption 必须写明 115/5465 的选择性质，并报告 115 只的行业构成（11 桶分 115 只，个别行业可能只剩两三只，Fig 4 的行业块解读会失真）。

**2. "估计 K̂≈1–2，展示 K=4"的内部张力现在有三处证据互相指认，不能再沉默。** Fig 2 图例逐年 K̂ 多为 1–2（2024:4、2025:5）；Table III 里行业和 FFC 的 GC₄ 双双垫底（0.28/0.12）——**第 4 因子谁都张不成**；Panel C 里第 4 因子跨样本构造最不稳（0.41）。但它偏偏又有最干净的单因子反转（盘中 −0.56/隔夜 +1.13）。处理：(a) 正文必须报告全样本 K̂_cont（pipeline 已算出，只差写进表格/正文一句话）；(b) Table II 加"First 4"块（循环里加个 K=4，两行代码）；(c) 叙事上把核心结论押在前 3 个因子，第 4 因子明确标注为 tentative。

**3. Table V 的 Industry 行疑似 12 资产切点 vs 其他行 4 资产——不可比。** 代码取 legacy CSV 里全部因子名（Market+11 行业），样本内最大夏普随资产数机械上升，Industry 的 1.55/2.71 相对 4 因子行被高估。且该行数值与上一版逐位相同（1.5498847819568…），证实是直接沿用旧口径。修法一行：切点集合限定为 Market+3 冻结行业（与 Table III 的 "Selected industry factors" 对齐）。跑之前先打开 `industry_factor_returns.csv` 数一下因子数确认。

**4. 隔夜 2.3–2.9 的切点夏普：机制已清楚，但呈现方式会误导。** 现在这组数不再是 rf 伪影——它数学上就是"做空市场隔夜"（F1 隔夜 SR=−2.27、MKT=−1.90）的样本内组合。必须在 caption 讲三点：全样本 in-sample；本质是 short-market-overnight；**在 T+1 和融券约束下不可实施**（Lou-Polk-Skouras 的机构/散户下单机制在 T+1 下也要重新表述）。顺带把最被低估的两行提到叙事 C 位："PCA overnight 权重放到盘中 SR=−1.34、PCA daily 权重放到隔夜 SR=−1.86"——**同一组权重跨段变号**，这是比任何散点图都干净的反转证据。

## 三、次级问题（声明或小修即可）

rf 残留不一致三处，均为二阶：行业因子/测试资产隔夜减全额日 rf（0/full）而 FFC MKT 是 20/24，差约 0.1bp/日；国债 CSMAR `Nrrdata` 分支按 /365 折日、逐交易日应用会少计约 1/3 的 rf（换国债数据时若走此分支需留意，或直接给 `date,rf_log_daily` 格式）；Fig 12 沿用 legacy 数据，行业面板 y 值被全额 rf 下移 ~0.6–0.8bp，占其 −2~−10bp 显示量级的 7–30%——趋势不变、水平有偏，caption 一句话即可。缓存有两处无签名兜底：`_load_or_build_rolling_outputs` 在签名不匹配时**优先复用旧 CSV 而非重算**（顺序反了，应对调）；`load_or_build_submission_figure_factor_counts` 同病。当前运行都是新算的，无实害，但换参重跑时会咬人。K̂ 判据在 N=4600 下的表现（2023–25 年 K̂=6–8、ER 贴着 1.08 爬）需要 footnote-28 式稳健性——`g_fn` 开关现成，挑 2015/2023 两年跑 logN/none 对照即可。涨跌停诊断仍缺席：a=3 下跳跃占比 1.4%（论文 0.5–0.9%）很可能含触板 bar，加一列"跳跃 bar 中触板占比"成本极低、说服力很高。

呈现层：**Fig 12/14 的行业点标成了 I01–I11 匿名码且全图无对照表**——讽刺的是 `_INDUSTRY_EN_LABELS` 英文映射就在同文件里，`_asset_label_map` 却刻意绕开了它，读者无从知道哪个点是哪个行业，改回英文名或加个 code→name 图例（Fig 4 反而做对了：图例内嵌、英文行业名、红虚线分隔，上一版的裁切/方框问题已解）。小项：Fig 13 图例的 "MKT_excess" 内部名外漏；Table II 可补 Figure 2 面板的 K̂ 行；Table I 可加 λ₁ 占比行（中国特征）。

## 四、值得升格为正文亮点的发现

这版数据里有四个真发现，别让它们埋在表里：(1) **FFC（尤其 SMB）张成 PCA 空间的能力不弱于 Market+3 行业**（GC₂ 0.929 vs 0.766），与原文"行业 ≫ 特征因子"正好相反，SMB-协动因子的故事经过重构后依然稳健——建议补一个 Market+SMB(+1 行业) 的显式消融把它钉死；(2) **A 股规模溢价在盘中**（+0.93/−2.32），与美股 SMB 在隔夜完全相反，价值在隔夜（+1.17）则与美股同向——干净的跨市场对照；(3) 2015 与 2024 的解释度尖峰（44–46%、33–36%）是论文 2008 叙事的 A 股对应；(4) Panel C 的 {0.86,0.73,0.60,0.41} 经模拟排除了构造伪影，可以作为幸存者偏差的定量度量来写。

一句话收束：这一版已经从"面板喂错了"进化到"方法链干净、剩下的是口径声明与可比性打磨"。四个系统性风险里只有 Table V 行业行(#3)需要改代码重出数，其余三个主要靠一次 391 稳健跑 + 正文诚实声明就能关闭。要不要我先把 #3 的补丁和 Table II 的 "First 4" 块一起写出来？