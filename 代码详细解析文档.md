# A 股 Pelger (2020) 高频 PCA 复现项目 · 代码详细解析文档

> 适用版本:交稿版仓库(`Reposit`,9 图 + 4 表口径)。
> 本文的目标是:一个没有接触过本项目的人,读完之后能够看懂仓库里每一个文件、每一个函数在做什么,知道每条关键代码对应论文的哪一处方法,并且知道日后要改动某个环节时应该动哪里、不该动哪里。
> 阅读建议:第 1~2 章建立全局观,第 3~9 章按数据流顺序逐层逐函数精读,第 10 章是"论文公式 ↔ 代码"对照总表,第 11~12 章是缓存机制与维护手册,遇到具体维护任务时直接查。

---

## 第 1 章 项目宏观框架

### 1.1 这个项目在做什么

本仓库把 Pelger (2020, *Journal of Finance*)《Understanding Systematic Risk: A High-Frequency Approach》的完整方法论迁移到中国 A 股:用 2013–2025 年全市场 5 分钟高频数据,先把收益分解为连续成分与跳跃成分,再用大维 PCA 从横截面中"统计地"提取系统性因子,估计因子个数,考察因子的经济含义(行业结构)、时间稳定性,最后检验这些因子在盘中、隔夜、日频三个时段的风险补偿与资产定价表现。

最终交付物固定为 **9 张图 + 4 张表**,全部落在 `Result/figures/` 与 `Result/tables/`:

| 产物 | 内容 | 论文对应 |
|---|---|---|
| Figure 1 | 逐年因子个数(年内平衡、跨年股票池变动) | 论文 Fig 1(非平衡面板) |
| Figure 2 | 逐年因子个数(13 年固定交集,N=115 恒定) | 论文 Fig 2(平衡面板) |
| Figure 4 | 连续 PCA 因子组合权重(按行业排序着色) | 论文 Fig 4 |
| Figure 7 | 21 日局部窗口权重与全样本权重的广义相关 | 论文 Fig 7 |
| Figure 10 | 因子结构时变分解(载荷 × 波动率) | 论文 Fig 10 |
| Figure 12 | 个股/行业/2×3 组合的盘中 vs 隔夜期望收益散点 | 论文 Fig 12 |
| Figure 13 | 三段(盘中/隔夜/日频)因子累计收益 | 论文 Fig 13 |
| Figure 14 | 行业组合资产定价(预测收益 + 定价误差) | 论文 Fig 14 |
| Figure 15 | 2×3 规模-价值组合资产定价 | 论文 Fig 15 |
| Table I | 连续/跳跃收益描述统计(4 档阈值 × 逐年 × 两种面板) | 论文 Table I |
| Table II | 两种面板口径的固定 K 广义相关 + K̂ 对照 | 论文 Table II |
| Table III | PCA 因子 vs 行业因子 vs FFC 的广义相关 | 论文 Table III |
| Table V | 盘中/隔夜/日频切点组合与单因子夏普比率 | 论文 Table V |

### 1.2 仓库目录结构

```
Reposit/
├── Code/
│   ├── main.py               # 唯一运行入口
│   ├── config.yaml           # 交稿运行配置(带全量中文注释)
│   ├── dataPrepare/          # 数据准备三步(抓取/预处理面板/5分钟动量)
│   ├── prepareCore/          # 核心库:配置、数学引擎、装配层、资产层
│   ├── figCode/              # 9 张图的渲染器(figure_01.py ... figure_15.py)
│   ├── tableCode/            # 4 张表的薄封装(table_i.py ... table_v.py)
│   └── devTools/             # 结构检查、产物校验、稳健性诊断
├── Result/
│   ├── figures/              # 9 张 PNG + industry_label_map.csv
│   └── tables/               # 4 张 CSV
└── Data/                     # (不入库)原始与加工数据,见 1.4
```

`prepareCore/` 是全项目的心脏,内部又分四个角色:`config.py`(运行配置)、`engine.py`(全部数学:面板、PCA、跳跃、K̂、GC、切点、定价,5651 行)、`submission_fast.py`(交稿装配层:把引擎输出组装成 4 张表和图的数据,1284 行)、`paper_tail.py`(资产层:行业因子、FFC、2×3 组合、图 12–15 的数据与渲染,2596 行)。另有 `paper_fidelity.py`(FFC 与 rf 拆分的论文保真实现)、`registry.py`/`runner.py`(任务注册与执行)、`io_utils.py`(路径与标题)、`pipeline_cache.py`(历史完整流程的结果缓存,交稿链路基本不再依赖)、`logging_utils.py`(统一日志)。

### 1.3 数据流四层全景

整个系统是一条单向流水线,读代码时始终记住自己在哪一层:

```
[第一层 数据准备 dataPrepare]
 原始 5 分钟 K 线(data.bz2, 每股一档) + 复权因子
   → step1_preprocess_panels.py
   → proc_Data/: 逐股收益 npz + universe.csv(样本资格标志)
                + panels/strict_balanced/{full, year_2013..year_2025}
                + panels/paper_lenient/{full, year_...}

[第二层 核心引擎 engine.py]
 面板(M×N 收益矩阵) → 跳跃识别 → 连续/跳跃拆分
   → 标准化 → PCA → K̂ 估计 → 因子/载荷/权重/三段因子收益

[第三层 装配层 submission_fast.py + paper_tail.py]
 引擎输出 + 外部资产(行业映射/FFC/rf/2×3 原材料)
   → Table I/II/III/V 的 DataFrame
   → Figure 12/13/14/15 的数据帧
   → 逐年因子数诊断、滚动窗口诊断、行业构成诊断

[第四层 渲染层 figCode + tableCode]
 数据帧 → matplotlib PNG / CSV → Result/
```

关键设计决策:**核心 PCA 每次运行现场重算**。115 只 × 3157 天的固定交集面板做一次全样本 PCA 只要分钟级,所以交稿链路不依赖任何历史 pickle,杜绝了"旧代码算的结果配新代码的图"这类版本混淆。只有三类东西会被缓存复用:预处理面板(第一层产物,重算要小时级)、paper_tail 资产层(行业因子/FFC 等,受 `refresh_paper_tail` 开关控制)、滚动窗口诊断(带签名校验,参数一变就自动重算)。

### 1.4 数据目录(Data/,不入 git)

- `Data/kline_Data/EXTRA_STOCK_A/<代码>/data.bz2`:原始 5 分钟 K 线,一股一档。
- `Data/proc_Data/pelger_cn_adjusted/`:预处理产物。`symbol_returns/` 下逐股 npz;`universe.csv` 是全部样本资格标志;`panels/` 下两种口径的面板(见第 3 章);`runtime/` 下是运行缓存、diagnostics、`export_summary.json`;`paper_tail/` 下是资产层缓存(行业因子收益、FFC 分段收益、2×3 组合收益、figure12 数据等 CSV)。
- `Data/external_Data/pelger_tail/`:外部补充数据。`industry/` 放行业映射两件套(`stock_full_info_std_industry_final.csv` 股票级、`行业映射表_终版.csv` 细分级);`factors/rf/risk_free.csv` 无风险利率(推荐格式 `date,rf_log_daily`);2×3 组合与动量的原材料。

### 1.5 如何运行

一切从 `python Code/main.py` 开始,行为由 `config.yaml` 决定,命令行参数可临时覆盖。最常用:

```
python Code/main.py                       # 按 YAML 生成 9 图 + 4 表
python Code/main.py --figures fig1,fig2   # 只重出指定图
python Code/main.py --tables table_v      # 只重出指定表
python Code/main.py --refresh-paper-tail  # 换 rf/行业映射后必须带一次
python Code/main.py --list                # 列出所有可用任务
python Code/devTools/check_submission_outputs.py   # 产物完整性自检
python Code/devTools/submission_diagnostics.py     # 稳健性诊断(g_fn 敏感性、lenient 对照等)
```

配置优先级:命令行 > `config.yaml` > 代码默认值。`config.yaml` 里每个键都带中文注释,此处不再重复;唯一要强调的是 `run.balanced_mode: strict_balanced` **不要改**,主结果固定用严格面板,宽松面板(`paper_lenient`)只作为 devTools 稳健性诊断用。

---

## 第 2 章 配置与调度层

### 2.1 main.py(339 行)——唯一入口

`main.py` 做五件事,按 `main()` 的执行顺序:

1. **解析配置**。`_load_yaml()` 读 `config.yaml`(容错:文件缺失时返回空 dict 走默认值);`_parse_args()` 定义全部命令行参数(`--stages/--figures/--tables/--data-steps/--list/--rf-file/--refresh-paper-tail/--no-fail-fast/--workers` 等);`_selected_stages/_selected_tasks/_selected_data_steps` 三个函数实现"命令行覆盖 YAML"的合并逻辑,内部都是"命令行给了就用命令行,否则用 YAML,再否则用默认"。
2. **构造 RunConfig**。`_build_run_config()` 把 YAML 的 paths/run 两节 + 命令行覆盖项灌进 `prepareCore.config.RunConfig`(见 2.2)。此处还会把 `SUBMISSION_FROZEN_INDUSTRIES`(事前冻结的三个行业因子桶:大金融、医药生物、周期资源)写入 `cfg.industry_factors_frozen`,并通过 `export_fidelity_env()` 导出为环境变量,供 `paper_tail.py` 在构建行业因子时读取。**行业冻结是硬编码在代码里的,不在 YAML 里,防止误改。**
3. **跑数据阶段(如有)**。`_run_data_steps()` 按 `data_steps` 顺序调用 `dataPrepare/step0/1/2` 的入口函数,参数(年份、股票数上限、动量窗宽等)从 YAML 的 `data:` 节读取。交稿默认 `data_steps: []`,即从不重跑数据准备。
4. **跑图表阶段**。`_resolve_render_tasks()` 把 `fig1...table_v` 这样的选择器交给 `registry.resolve_keys()` 换成 Task 对象列表;`_run_render_tasks()` 是核心:它**只调用一次** `build_submission_fast_result(cfg)` 得到共享的 `ReplicationResult`,然后循环把它喂给每个任务的 `generate(result, cfg)`。这样 9 图 4 表全部基于同一次 PCA、同一套数据,不存在任务间口径漂移。任一任务抛错时,`fail_fast=True` 立即终止,否则记入 failures 继续。
5. **写运行摘要**。`_write_export_summary()` 把每个任务的输出路径与失败清单写进 `runtime/export_summary.json`。交稿前检查该文件 `failures` 必须为空。

维护提示:新增一张图/表时,main.py 不用改,只需在 `registry.py` 注册 Task、在 figCode/tableCode 写渲染器(见 12.3)。

### 2.2 prepareCore/config.py(401 行)——RunConfig 与运行档位

`RunConfig` 是贯穿全项目的配置数据类,所有函数都只认它,不直接读环境变量或 YAML。字段分五组:

- **路径组**:`proc_root / runtime_root / final_result_root / external_data_root / paper_tail_root / rf_file`。相对路径以仓库根为基准。
- **估计参数组(方法论核心,轻易不要动)**:`jump_a = 3.0`(跳跃阈值系数 a,论文主结果档)、`jump_omega = 0.49`(阈值幂指数 ω̄)、`k_max = 10`(特征值比检验的最大候选因子数)、`gamma = 0.08`(显著性阈值 γ)、`g_fn = "median_sqrtN"`(扰动函数 g(N,M)=√N·median(λ),论文口径;另有 `logN/none` 两档供敏感性)、`DISPLAY_CONTINUOUS_FACTOR_COUNT = 4`(展示用因子数,详见 4.9)、TOD 相关参数(时段波动模式的估计窗)。
- **样本口径组**:`balanced_mode = "strict_balanced"`(主口径:严格面板)、`sample_start/sample_end`(2013-01-04 ~ 2025-12-31)。
- **行为组**:`fail_fast / refresh_paper_tail / strict_final_export / restart / rebuild_result`。
- **资源组**:`workers / panel_workers / paper_workers / rolling_workers / memory_budget_gb`,空值表示用代码默认。

`cache_signature` 属性把"会改变数值结果"的参数拼成签名(面板口径、jump_a、k_max、gamma、g_fn、收益方案),供滚动缓存与历史缓存做一致性校验;**刻意不包含**纯呈现类开关,这样改个图例颜色不会作废缓存。`MainLaunchProfile` 及相关函数是历史完整流程的运行档位机制,交稿链路已不走它,读代码时可以略过。

### 2.3 registry.py / runner.py / io_utils.py / logging_utils.py

- `registry.py`(106 行):`Task` 数据类(key、种类 figure/table、编号、generate 函数引用)+ 全部 13 个任务的注册表。`resolve_keys(["fig1","table_v"])` 把选择器解析为 Task 列表,支持 `figures/tables/all` 这类组选择器。
- `runner.py`(79 行):`run_generator()` 统一包装单任务执行(计时、日志、异常捕获并返回 traceback 文本);`run_standalone()` 让每个 figure_XX.py 可以 `python Code/figCode/figure_04.py` 单独运行(仅调试用,交稿产物一律走 main.py,原因见 11.3)。
- `io_utils.py`(137 行):`FIGURE_SPECS` 字典集中定义每张图的文件名与标题(Figure 1/2 的标题已按新面板语义改为 "Yearwise-Balanced Changing Universe" / "Fixed-Intersection Balanced Panel");`figure_path/table_path/diagnostics_dir` 统一产物落盘位置。改图名/标题只改这一处。
- `logging_utils.py`:统一的 `[时间戳][TAG][KIND] message` 日志格式与 `Heartbeat`(长任务心跳线程,防止外部以为进程卡死)。

---

## 第 3 章 数据准备层 dataPrepare

### 3.1 step0_get_apidb.py——原始数据抓取

从本机数据服务批量拉取 A 股 5 分钟 K 线并落盘为 `data.bz2`(逐股一档,含 open/high/low/close/volume/amount 与时间戳)。交稿环境不重跑;若要扩充样本,注意它有断点续抓与限速逻辑,`--workers 1` 起步。

### 3.2 step1_preprocess_panels.py——从原始 K 线到可用面板(全项目地基)

这是数据层最重要的脚本,产出三样东西:逐股收益 npz、`universe.csv` 资格标志、两种口径的面板。按内部流程讲:

**(1) 逐股处理**。对每只股票:读 `data.bz2` → 用复权因子做**后复权**(保证收益跨除权日可比)→ 对齐到全局交易日历与统一的 5 分钟 bar 网格(A 股每日 4 个交易小时,48 根 bar)→ 计算对数收益。收益方案是 `daily_intra_night_total_plus_full_5min_v1`(常量 `PELGER_INTRADAY_ONLY_5MIN` 控制):**5 分钟序列只含盘中**,首根 bar 用"当日开盘价→首根收盘价"(开盘集合竞价的信息进首根 bar),**隔夜收益单独存一列**(昨收→今开),另存日内合计、日频合计。这样"盘中连续+盘中跳跃"与"隔夜跳跃"天然分离,对应论文把隔夜建模为一个独立跳跃的处理(Bollerslev–Li–Todorov 2016)。

**(2) universe.csv 资格标志**。`_summarize_processed_universe()` 对每只股票逐年统计有效天数与 bar 覆盖率,生成一组布尔列,后续一切样本选择都查这张表:
- `is_strict_{year}`:该年**每个交易日、每根 bar 都齐**(年内全勤)。
- `is_paper_{year}`:该年覆盖率 ≥ 阈值(仿论文附录 A 的宽松口径,缺口后续插值为零增量)。
- `is_strict_balanced`:2013–2025 **所有** `is_strict_{year}` 同时为真(13 年全勤交集,实际 115 只)。
- `is_balanced_paper`:13 年宽松口径的交集(391 只)。

**(3) 面板写盘**。`_select_sample_rows(universe, sample_mode, years)` 是选样的唯一入口,逻辑必须记清:`years=None` 时取全样本交集列(`is_strict_balanced` 或 `is_balanced_paper`);`years=[y]` 时取单年列(`is_strict_{y}` 或 `is_paper_{y}`)。随后 `_write_panel()` 把选中股票的收益堆成 M×N 矩阵存 npz,并写 `full.json / year_YYYY.json` 元数据(dates、tickers、bar_times、收益方案、样本报告)。于是:

- `panels/strict_balanced/full` = **固定交集平衡面板**(全项目主面板,N=115 恒定,正文 Figure 2 语义);
- `panels/strict_balanced/year_YYYY` = **逐年年内平衡面板**(跨年股票池变动,N 逐年 1319/988/557/…/4635,正文 Figure 1 语义);
- `paper_lenient/` 同结构的宽松版(N=391 等),仅供稳健性。

严格面板**不含任何 NaN**(全勤定义保证),这是后面引擎可以放心走全矩阵 PCA 的前提。

### 3.3 step2_build_mom_5min.py——5 分钟动量因子

滚动 48 根 bar(跳过最近 1 根)排序,做 30%/30% 赢家输家组合,产出 5 分钟频率的动量因子序列,供 FFC 的 Carhart MOM 使用。参数在 YAML 的 `data.mom_5min` 节。

---

## 第 4 章 核心数学引擎 engine.py(5651 行)

engine.py 前 ~800 行是并行与内存的工程设施(进程池、BLAS 线程钳制、内存预算、CheckpointManager、进度上报),与方法论无关,维护时除非碰到资源问题不必细读。方法论从面板加载开始,以下按数据流顺序逐函数讲透。

### 4.1 HFPanel 与 load_proc_hf_panel()——面板加载

`HFPanel` 是内存中的面板对象:`R_5min_full`(M×N,盘中 5 分钟收益)、`R_intra/R_night/R_daily`(D×N,日级三段)、`dates/tickers/bar_times/day_ids`(每根 bar 归属哪个交易日的索引)、`N/D/M` 维度属性。`load_proc_hf_panel(cfg, sample_mode, years)` 按 `_proc_panel_paths()` 拼路径读 npz+json;`VALID_PANEL_SAMPLE_MODES = {strict_balanced, paper_lenient}` 两种都放行。`subset_panel_by_years(panel, years)` 用 day_ids 掩码把 full 面板切成某几年的子面板(Figure 2 的逐年切片就是这么来的,**不是**重新选股)。

### 4.2 detect_jumps()——TOD 自适应阈值跳跃识别(论文式 (10) 一带)

目的:把每根 5 分钟收益判为"连续"或"跳跃"。实现分三步:

1. **日内时段波动模式(TOD)**:对每个 bar 时点 j,用全样本该时点的收益平方均值除以全天平均,得到 `TOD_j`(开盘/收盘时段天然更波动,阈值应更宽,避免把开盘冲击误判为跳跃)。
2. **逐日波动基准**:对每只股票每天算 Bipower Variation(相邻收益绝对值乘积的尺度化和,对跳跃稳健的波动估计),记 `BV_day`。
3. **阈值判定**:`|r_{i,j,day}| > a · Δ^ω̄ · sqrt(BV_day · TOD_j)` 即标为跳跃,其中 Δ=1/48(bar 占一天的比例)、ω̄=0.49、a=`cfg.jump_a`(主结果 3,Table I 同时报 4/4.5/5)。函数返回跳跃布尔掩码,调用方由此得到 `R_cont = R·(1-mask)`、`R_jump = R·mask`(被判跳跃的 bar 在连续矩阵中置零,反之亦然)。

### 4.3 _standardize_returns_for_pca()——标准化(论文用相关阵做 PCA 的实现)

对每列(每只股票)除以其 `sqrt(Σ r²)`(实现上写成除以 √M 后的均方根,数学等价)。除的是**二次变差的平方根**而非样本标准差,和论文一致(高频下均值项可忽略,二次变差就是波动)。标准化后 `Rs' Rs / N` 的对角线全为 1/N 的常数倍,等价于对相关性矩阵做特征分解,避免高波动股票主导第一主成分。函数同时返回每列的尺度 `scales`,供后面把特征向量换算回"可作用于原始收益的组合权重"。

### 4.4 pca_factors()——核心 PCA(论文式 (前后) 的三行数学)

```
S  = Rsᵀ Rs / N          # N×N,标准化收益的(伪)相关阵
λ, V = eigh(S)            # 特征分解,取前 K 大
Λ̂  = √N · V[:, :K]        # 载荷(论文的规范化 Λ̂ᵀΛ̂/N = I)
F̂  = Rs · Λ̂ / N           # 因子时间序列(M×K)
```

返回 `PCAResult(Lambda, F, eigvals, scales, ...)`。注意两点:其一,特征向量是**嵌套的**,K=4 的前 3 列与 K=3 的结果完全相同,所以"估计 K̂、展示 4"不会互相污染;其二,这里的 F̂ 是标准化收益空间的因子,要得到"真实收益空间的可交易组合"需经 4.7 的权重换算。

### 4.5 pca_factors_pairwise() 与 _panel_pca()——含缺失面板的兜底

`pairwise_available_covariance()` 用逐对共同观测均值算交叉积(对角线因此不再是 1),`psd_fix` 把负特征值截零后再分解。**严格面板永远不会触发它**:`_panel_pca()` 只在矩阵含 NaN 时才降级到 pairwise,交稿主链全部走 4.4 的全矩阵路径。这个兜底只服务于宽松面板的诊断场景,其统计性质弱于全矩阵 PCA,不要把它的结果当主结果。

### 4.6 perturbed_eigenvalue_ratio()——因子个数估计(论文式 (11))

```
g   = median(λ₁..λ_N) · √N        # 扰动项 g(N,M),g_fn="median_sqrtN"
ERₖ = (λₖ + g) / (λₖ₊₁ + g)        # 扰动特征值比
K̂   = max{ k ≤ k_max : ERₖ > 1+γ } # γ=0.08,取**最后一个**越线的 k
```

三个实现细节:分母加 g 抑制小特征值的比值爆炸;K̂ 取"最后一个"而非"第一个"越线位置,所以 ER₁ 再大也不会淹没后续因子(A 股市场因子极强,ER₁ 常达 5~16,这是市场特征不是 bug);`g_fn` 支持 `logN/none` 两档,`devTools/submission_diagnostics.py` 会对代表性年份出 3×3 敏感性表。

### 4.7 orient_pca_result() 与 factor_portfolio_weights()

- **符号定向**:特征向量符号任意,`orient_pca_result()` 约定第一因子载荷和为正(市场因子朝上),其余因子按载荷绝对值最大分量为正,保证跨运行、跨图表符号一致。
- **权重换算**:`W = Λ̂ / (√N · scales)`,把标准化空间的载荷除回每列尺度,得到**直接作用于原始收益**的组合权重:`F_display = R_daily · W` 就是日频因子收益,`R_intra · W`、`R_night · W` 同理。三段共用同一 W,自动满足 盘中因子+隔夜因子=日频因子 的会计恒等。

### 4.8 generalized_correlations()——广义相关(论文比较因子集的工具)

对两组因子 F(M×K₁)、G(M×K₂):各自协方差白化(`_inv_sqrt_psd` 做对称逆平方根),对交叉协方差做 SVD,奇异值即典型相关系数,返回前 min(K₁,K₂) 个,按降序。性质:对任一侧的可逆线性变换不变(所以旋转、缩放、换基都不影响 GC),这也是 Table III 能安全使用"除以日频 std 的标准化因子"作比较基准的数学依据。

### 4.9 PelgerPipeline.run_full()——引擎总装

一次调用完成主面板的全部估计,顺序:加载面板 → `detect_jumps` 拆分 R_cont/R_jump → 分别算全谱特征值 → `perturbed_eigenvalue_ratio` 得 `K_cont_hat / K_jump_hat / K_hf_hat` → 按 K̂ 做 `pca_cont/pca_jump` → 另做 `pca_cont_display`(固定 K=4;若 K̂≥4 直接截前 4 列,否则重算 K=4,`_truncate_pca_result` 负责截取)→ 符号定向 → 权重 → 三段展示因子收益 `F_cont_display_{intra,night,daily}`。**"估计与展示分离"是刻意设计**:K̂ 是统计证据(写进 manifest 与 Table II),展示层为对齐论文固定用 4 因子;两者的张力(A 股逐年 K̂ 常为 1~2)在正文如实讨论。

### 4.10 逐年因子数诊断:_factor_count_rows_for_panel() 与两个 builder

`_factor_count_rows_for_panel()` 对一个面板矩阵算全谱特征值 + K̂ + er_1..er_10,分 continuous/jump/hf 三个成分各出一行。`build_submission_figure1_factor_counts()` 循环 13 年,每年 `load_proc_hf_panel(..., years=[y])` 加载**年内平衡面板**(Figure 1 语义);`build_submission_figure2_factor_counts()` 则对 full 面板做 `subset_panel_by_years` 逐年**切片**(Figure 2 语义,N 恒 115)。两者产物落 `runtime/submission_fast/diagnostics/figure{1,2}_factor_counts.csv`,图 1/2 与 Table II 的 K̂ 行共用。

### 4.11 tangency_portfolio() 与 time_series_pricing()

- `tangency_portfolio(F, rf)`:`w ∝ Σ⁻¹μ`,切点组合的样本内最大夏普 `SR = √(μᵀΣ⁻¹μ)`(恒非负;跨段迁移权重后的带号 SR 由调用方用组合序列的 mean/std 计算)。
- `time_series_pricing(assets_excess, F)`:对每个测试资产做带截距 OLS,`α = 截距`(定价误差)、`预测收益 = βᵀ·mean(F)`,即可交易因子模型的标准时序检验,直接供 Figure 14/15。

### 4.12 refresh_replication_result_views()

给 ReplicationResult 重建"展示视图":display PCA 截取/重算、符号定向、W_display、三段展示因子收益,并在 `refresh_paper_tail=True` 时联动重建资产层(见 6.1)。装配层每次运行都会经过它,保证展示层永远来自当前代码。

---

## 第 5 章 交稿装配层 submission_fast.py(1284 行)

这一层把引擎输出组装成最终交付物的数据。读它抓住一个总函数、四个表构建器、三个基础设施即可。

### 5.1 build_submission_fast_result()——每次运行的总装配

执行顺序(也是 main.py 每次运行真实发生的事):

1. `load_proc_hf_panel(cfg, STRICT_BALANCED_SAMPLE, years=None)` 加载 115 只主面板;非 strict 口径直接 raise(主结果口径锁死)。
2. `PelgerPipeline(...).run_full()` 现场跑完整估计(见 4.9),分钟级。
3. `_load_or_build_rolling_outputs()` 取滚动窗口诊断(带签名校验的缓存,见 5.4)。
4. `build_submission_figure{1,2}_factor_counts()` 重算并覆写逐年因子数诊断 CSV。
5. `write_strict_panel_industry_composition()` 输出 115 只的行业构成诊断(`strict115_industry_composition.csv`,含 UNMAPPED 兜底),供正文讨论幸存者倾斜。
6. 组装 `ReplicationResult`(pipeline、rolling、diagnostics 路径等)并写 `submission_fast_manifest.json`,**manifest 里落了 `K_cont_hat / K_jump_hat / K_hf_hat`**,这是正文引用全样本 K̂ 的权威出处。
7. `build_submission_fast_tail_payload()` 装载资产层:行业因子收益、FFC 分段收益、2×3 组合、figure12 数据这四类**从 paper_tail 缓存 CSV 读取**(`refresh_paper_tail=False` 时复用);figure13 数据与定价数据帧则**基于本次新鲜 pipeline 现算**。

### 5.2 四个表构建器

**build_submission_table_i_aligned()**:两个 panel_block,严格对齐图 1/2 语义。对每年 × 每个阈值 a∈{3,4,4.5,5}:调 `detect_jumps` 后统计四个量(跳跃增量占比、跳跃解释的 QV 份额、第一跳跃因子解释的跳跃相关份额、前四连续因子解释的连续相关份额,后两者来自相应相关阵的特征值占比)。Figure 1 块逐年加载年内平衡面板现算;Figure 2 块对 full 面板逐年切片。列含 `n_symbols/n_days/sample_semantics`,自带口径自证。

**build_submission_table_ii_paper_style()**:逐年把两种面板对齐到同一批交易日,各做 `_panel_pca(K=4)`,对连续与跳跃成分分别计算 First 2/3/4 因子的 GC(固定 K 比较,回答"小面板能否代表大面板的因子空间");另附四行 Panel metadata:**两种面板各自的逐年 K̂** 与 N。行序:metadata → 连续 GC 块 → 跳跃 GC 块。注意 2019 这类年份 K=2 时 GC₂ 会塌到近 0 而 K=3 恢复,这是特征值接近导致的排序交换,属解读事项(表注已写),不是 bug。

**build_submission_table_iii()**:基准 = 主面板 4 个连续 PCA 因子的**日频**收益(从 figure13 数据反推的标准化序列,GC 对缩放不变故合法)。三个 Panel:A 经济解释(Market、三个冻结行业因子各自单因子 GC + Market+3 的四因子集 GC);B 传统因子(FFC 四因子集,样本 2014-07 起,NaN 行成对删除);C 样本构造稳健性(逐年对齐的 changing-universe PCA 四因子,见 6.3)。

**build_submission_table_v()**:三段夏普总表。分四个 section:`factor_set_tangency`(Continuous PCA / 对齐 PCA / Industry / FFC 各自按段独立估计切点,另加两行**权重迁移**:`PCA (overnight-tangency weights)` 用隔夜权重在三段上评估、`PCA (daily-tangency weights)` 同理,负号即反转的直接证据);三个单因子 section(连续四因子、FFC 四因子、对齐四因子)。**rf 口径**:PCA 系(零成本组合)不减 rf,遵循论文 footnote 33;FFC 的 MKT 在构建期已内嵌 4/24–20/24 拆分的 rf;行业因子(多头组合)经 `_to_excess_matrices` 减 rf(盘中 0/隔夜全额)。三组口径差异在表注声明,量级为 0.1bp/日级,二阶。年化用 `_annualization_days()=244`。

### 5.3 figure13 数据与反推:_standardized_return_mats_from_figure13()

Table III/V 的 PCA 因子矩阵不是重新算的,而是把 figure13 数据帧(归一化累计收益)按 `pivot → diff` 反推回逐日收益。合法性依赖一个关键约定:**figure13 对同一因子的盘中/隔夜/日频三段统一除以该因子的日频 std**(见 6.4)。同一对角缩放下,单因子 SR、同段切点 SR、跨段迁移 SR 全部精确不变(缩放在 Σ⁻¹μ 中抵消)。**维护红线:若有人把 figure13 改成"逐段各自标准化",Table V 的跨段行会静默变错。**

### 5.4 滚动诊断缓存:_load_or_build_rolling_outputs()

优先级:runtime checkpoints(签名匹配时逐块拼装)→ submission_fast 诊断 CSV 缓存(**读取时校验 expected_signature**,含 k_cont_hat/g_fn/gamma/面板口径等,不匹配即弃用)→ 现场重算 `rolling_gc_and_explained_variation`(21 日窗、步长 1、K=K_cont_hat,对全样本 Λ 求逐窗 GC 与解释度)并连签名一起持久化。签名机制保证改参数后旧缓存自动失效。

### 5.5 其他基础设施

`_long_to_factor_mats()` 把长表因子 CSV 按 sample_dates 透视成 段→(D×K) 矩阵字典;`_load_industry_lookup_for_cfg()` 读行业映射(兼容 ts_code/code 两种列名,统一大写去空格);`_drop_invalid_rows/_align_matrix` 做 NaN 行成对删除与日期对齐;`_write_csv/_write_json` 统一 utf-8-sig 落盘。

---

## 第 6 章 资产层 paper_tail.py(2596 行)

paper_tail 管两件事:**造资产**(行业因子、FFC、2×3 组合、figure12 数据,统称 legacy 资产层,产物是 `Data/proc_Data/.../paper_tail/` 下的 CSV)与**画图 12–15**。

### 6.1 legacy 资产层的构建与刷新

`refresh_paper_tail_views()`(由 `refresh_replication_result_views(refresh_paper_tail=True)` 触发,即 `--refresh-paper-tail`)重建全部资产 CSV:

- **行业因子** `_build_industry_factor_frame()`:Market = 全市场**等权**日收益;每个冻结行业 = 该行业**等权**组合收益,三段各一列。行业选择走 `_industry_frozen()`(环境变量,由 main.py 的 SUBMISSION_FROZEN_INDUSTRIES 注入),只有环境变量缺失才落到旧的按集中度自动挑选兜底。产物 `factors/industry_factor_returns.csv` 恰含 Market+3 共 4 个因子。
- **FFC 分段因子**(实现在 paper_fidelity,见第 7 章):`factors/ffc_segmented_returns.csv`,2014-07 起。
- **2×3 规模-价值组合与 14 行业测试资产**:`assets/` 下,逐年重分组的 size×B/M 六组合与行业组合三段收益。
- **figure12 数据**:对每只股票/每个组合算样本期 `mean_intraday_excess = mean(盘中收益)`(不减 rf)与 `mean_overnight_excess = mean(隔夜收益 − 全额日 rf)`。x 轴不减、y 轴减全额的不对称在图注声明(反转斜率不受影响)。

**刷新纪律:凡替换 rf 文件、行业映射、2×3 原材料,下一次运行必须带 `--refresh-paper-tail`**,否则 FFC 内嵌旧 rf 会与新口径混用(表注/README 均已写明)。`_load_rf_series()` 兼容三种 rf 格式:`date,rf_log_daily`(推荐)、CSMAR 日利率 `Clsdt,Nrrdaydt`、CSMAR 年利率 `Clsdt,Nrrdata`(后者按 /365 折日,逐交易日应用会少计约三分之一,换国债数据时优先用推荐格式绕开)。

### 6.2 _segment_rf_vectors() 与 _to_excess_matrices()

给多头组合(行业因子、测试资产)减 rf 的工具:盘中 0、隔夜全额日 rf、日频全额。与 FFC 的 4/24–20/24 拆分(第 7 章)存在约 0.1bp/日的口径差,属声明级差异。PCA 因子**不经过**这里(零成本,不减 rf)。

### 6.3 逐年对齐 changing-universe PCA:_build_yearly_aligned_unbalanced_pca_segments()

论文"用变动股票池检验幸存者稳健性"的实现,也是全仓库数学上最精巧的一段:

1. 逐年取两个面板:full 切片(115 只)与年内平衡面板(当年全勤股);各做 K=4 连续 PCA、定向、取权重,三段因子收益 = 各段收益 @ 权重。
2. `_orthogonal_rotation(x, y)`:Procrustes 解,`u,_,vt = svd(xᵀy)`,`q = u·vt`,即最小化 ‖xq − y‖ 的正交旋转。
3. 双旋转链:`q1` 把当年 changing 因子旋到当年 fixed 因子;`q2` 把当年 fixed 因子旋到**全样本**展示因子(限当年行);总旋转 `q = q1·q2`,三段共用(线性性保证 盘中+隔夜=日频 恒等保持)。
4. 13 年拼接成整段序列,即 Table III Panel C 与 Table V "unbalanced, yearly aligned" 行的来源。受控模拟已验证:因子空间真同源时该构造能恢复 GC≈1,故实测 {0.86,0.73,0.60,0.41} 是真实的幸存者敏感度信号。

### 6.4 figure13 数据帧

对每个因子集(Continuous PCA / 对齐 PCA / FFC)×每段:算逐日因子收益 → **除以该因子日频 std(三段同一分母)** → 累计求和(首个有效日起,中途缺失记零增量)。这一约定是 5.3 反推合法性的根基,不要动。

### 6.5 图 12–15 渲染器与行业标签

- **Fig 12**:三面板散点(全部个股/11 行业组合/2×3 组合),带趋势线与零轴。
- **Fig 13**:3 因子集 × 3 段的九宫格累计收益,四因子四线。
- **Fig 14/15**:上半 Panel A 预测收益 vs 实现收益散点(45 度线),下半 Panel B 逐资产 α 柱状;PCA 与 FFC 两行对照;`fig.text` 定位 Panel 标题并按行数自适应留白(旧版标题重叠已修)。
- **行业标签**:`_asset_label_map()` 现直接映射到 `_INDUSTRY_EN_LABELS` 的英文名(Financials/Healthcare/…),并把映射表导出为 `Result/figures/industry_label_map.csv` 备查;figCode 侧的 `_industry_display_label` 同源。中文字体问题被"标签英文化"整体绕开,环境无需装 CJK 字体。

---

## 第 7 章 paper_fidelity.py(381 行)——FFC 与 rf 拆分的论文保真件

- `split_daily_rf(rf, intraday_hours=4, calendar_hours=24)`:日 rf 按 4/24 归盘中、20/24 归隔夜(A 股 4 个交易小时)。
- `build_full_market_size_value()`:全市场逐年 6 月末按市值中位数 × B/M 三分位重分组,产出 2×3 组合三段收益(SL/SM/SH/BL/BM/BH),SMB/HML 由此差分,**股票级先分段再组合**,保证盘中+隔夜=日频在因子层严格成立。
- `build_full_market_momentum()`:Carhart 口径动量(过去 11 个月跳过最近 1 个月),同样股票级分段。
- `build_ffc_segmented_clean()`:总装 FFC 四因子。MKT = **市值加权**全市场收益 − rf(按上面的 4/24–20/24 拆段);SMB/HML/MOM 为多空零成本,不减 rf。样本自 2014-07 起(B/M 数据可得性)。注意:FFC 的"市场"是 VW 减 rf,行业因子组的"Market"是 EW 不减 rf,同表两种市场定义在 Table V 表注中声明。

---

## 第 8 章 渲染层 figCode

每张图一个文件,统一模式:`generate(result, cfg)` 取数 → 私有 `_render()` 画图 → `_save`(atomic 落盘)→ 返回输出路径;文件末尾 `run_standalone` 支持单跑调试。

- **figure_01/02.py**:读 5.4 产出的因子数诊断 CSV(读取处校验 g_fn/gamma 与当前 cfg 一致,防陈旧),按 4 年一组分面;每条年曲线画 er_1..er_10,图例含 `(K=K̂, N=n)`,并在 K̂ 位置标注 `K=x` 彩色文字;绿线为临界值 1+γ=1.08。Figure 1 的 PANEL_BLOCK="Yearwise-balanced changing universe",Figure 2="Fixed-intersection balanced, yearly slices",与 builder 写入的 panel_block 字符串严格相等(改一处必须同步另一处)。
- **_weights.py + figure_04.py**:取 `pipe.pca_cont_display`,V=Λ/√N 为纵轴;股票按行业排序,行业着色,红虚线分隔;图例内嵌画布右侧(`bbox_to_anchor=(0.835,0.5)` + `tight_layout rect` 预留 18% 右缘),行业名走英文映射。
- **_timevar.py + figure_07/10.py**:`local_pca_windows()` 以 21 交易日为窗、步长 1,对 `result.pipeline.R_cont` 现场做局部 PCA;Fig 7 画 K=7 个局部权重对全样本权重的 GC 曲线(七线);Fig 10 画 3×2 分解(系统性影响/平均载荷/因子波动率 × 原始/归一化),对应论文 III.E 的"固定全局因子 + 局部回归载荷"估计。
- **figure_12/13/14/15.py**:薄封装,把 tail payload 的数据帧转交 paper_tail 的渲染函数。

## 第 9 章 tableCode 与 devTools

- **tableCode/table_{i,ii,iii,v}.py**:每个不足 30 行,调用 submission_fast 对应 builder → `_write_csv` 到 `Result/tables/`。表的一切逻辑都在 builder,改表不要改这层。
- **devTools/check_project_structure.py**:目录/文件存在性体检。
- **devTools/check_submission_outputs.py**:9 图 4 表的存在性、非空、关键列与行块完整性校验,交稿前必跑,期望全 OK。
- **devTools/submission_diagnostics.py**:三组稳健性诊断,产物落 diagnostics:`g_fn_sensitivity_3x3.csv`(代表性年份 × {median_sqrtN,logN,none} 的 K̂ 对照)、涨跌停代理诊断(被判跳跃的 bar 中触及涨跌停量级的占比,解释 A 股跳跃占比偏高)、`Table_III/V_robustness_lenient391.csv`(宽松 391 面板的最小 PCA 对照,legacy 资产复用,仅供稳健性引用)。

---

## 第 10 章 论文方法 ↔ 代码实现 对照总表

| 论文位置 | 方法 | 代码落点 |
|---|---|---|
| 式(1)–(3) 局部近似因子模型 | R = ΔF Λᵀ + Δe | `pca_factors()`;"局部"体现在逐年/逐窗分别估计 |
| 式(4)–(5) 三段分解 | 连续+盘中跳跃+隔夜跳跃 | 预处理的收益方案(3.2)+ `detect_jumps`(4.2) |
| 阈值跳跃识别(BLT2013 TOD) | a·Δ^0.49·√(BV·TOD) | `detect_jumps()`,a=jump_a,ω̄=jump_omega |
| 相关阵 PCA、Λ̂=√N V、F̂=RΛ̂/N | 主估计量 | `_standardize_returns_for_pca` + `pca_factors` |
| 式(11) 扰动特征值比 | g=√N·median,γ=0.08 | `perturbed_eigenvalue_ratio()` |
| 广义相关(Bai–Ng) | 典型相关比较因子空间 | `generalized_correlations()` |
| 变动股票池稳健性 | 逐年估计旋回全样本基准 | `_build_yearly_aligned_unbalanced_pca_segments()` |
| 载荷时变分解(III.E) | 固定全局因子+局部载荷 | `_timevar.py` 的 fig10 路径 |
| 切点组合/最大夏普(Barillas–Shanken) | SR=√(μᵀΣ⁻¹μ) | `tangency_portfolio()` + Table V builder |
| footnote 33 零成本因子不减 rf | rf 口径 | Table V:PCA 系不减;FFC MKT 4/24–20/24(`split_daily_rf`);行业 0/全额(`_segment_rf_vectors`) |
| 时序定价检验 | α=截距,预测=β·mean(F) | `time_series_pricing()` + Fig 14/15 |
| Table AI 行业组合 | SIC → 行业 | 行业映射两件套 + `_build_industry_factor_frame`;110 细分→11 主行业,冻结 3 桶 |

## 第 11 章 缓存与一致性机制(为什么结果不会"串版本")

1. **核心 PCA 零缓存**:每次 main.py 现场重算,展示层经 `refresh_replication_result_views` 重建,天然与当前代码一致。
2. **滚动诊断带签名**:签名含面板口径、g_fn、gamma、jump_a、k_cont_hat 等;checkpoints 与 CSV 缓存读取时都校验,不匹配即重算并覆写。
3. **逐年因子数诊断每次覆写**:build 阶段直接重算落盘,渲染器读取时再校验参数一致性,双保险。
4. **paper_tail 资产层显式开关**:`refresh_paper_tail` 默认 False 以复用;凡外部输入(rf/行业映射/2×3 原材料)变化必须显式刷新一次。这是全系统唯一需要"人记得"的一致性点,已在 config.yaml、README、表注三处提示。
5. **export_summary.json + manifest**:每次运行的任务输出、失败清单、K̂ 三件套、面板规模全部留痕,交稿核对以此为准。
6. **run_standalone 仅调试**:单跑图任务可能绑定回退缓存,交稿产物一律 main.py 统一生成。

## 第 12 章 维护手册

### 12.1 常见改动场景速查

| 场景 | 操作 | 注意 |
|---|---|---|
| 更换无风险利率(如 SHIBOR→国债) | 替换 `factors/rf/risk_free.csv`(推荐 `date,rf_log_daily` 格式)→ `python Code/main.py --refresh-paper-tail` | 不刷新则 FFC 内嵌旧 rf,同表混口径;刷新后 diff 一下 `ffc_segmented_returns.csv` 的 MKT 列确认生效 |
| 更换/微调行业映射 | 替换 industry 两件套 → `--refresh-paper-tail` | 冻结三桶名称若变,同步改 main.py 的 `SUBMISSION_FROZEN_INDUSTRIES` |
| 调 γ / g_fn / jump_a | 改 `config.py` 对应默认或临时改 RunConfig | 滚动缓存因签名自动失效重算;Table I 的阈值档在 builder 内 `(3,4,4.5,5)` 元组处改 |
| 扩年份(如加 2026) | 补原始 K 线 → `--stages data --data-steps preprocess_panels` → 正常跑 | universe 标志、两种面板、逐年诊断全链会自动覆盖新年份 |
| 只重出某张图/表 | `--figures figN` / `--tables table_x` | 核心 PCA 仍会现场重算一次(分钟级),这是特性不是浪费 |
| 出宽松面板稳健表 | `python Code/devTools/submission_diagnostics.py` | 产物在 diagnostics,只作附录引用,不替换主表 |
| 新增一张表 | submission_fast 写 builder → tableCode 写 wrapper → registry 注册 → config.yaml 的 tables 列表加 key | 输出路径用 `_table_x_output_path` 同款模式 |

### 12.2 陷阱清单(动手前先读)

1. **两种"平衡"别混**:full=13 年固定交集(N 恒 115);year_YYYY=年内全勤(N 逐年变)。Figure 1/2、Table I/II 的语义命名与此严格绑定。
2. **figure13 的"三段同一 std"约定**是 Table V 反推的前提,改归一化必须同步改 Table V 取数方式。
3. **切点 SR 是全样本 in-sample**,隔夜高 SR 本质是"做空市场隔夜";正文与表注必须保留 T+1/融券不可实施的声明。
4. **同表两种"Market"**:行业组 EW 不减 rf,FFC 组 VW 减 rf,勿直接横比水平。
5. **pairwise PCA 只兜底**,严格面板永不触发;若日志出现 pairwise 字样,说明面板混入了 NaN,先查数据。
6. **年化天数 244**(`_annualization_days`),与美股 252 不同,复算夏普时别用错。
7. **面板字符串常量成对出现**(builder 写入的 panel_block ↔ 渲染器的 PANEL_BLOCK),改名必须两处同步。
8. **K̂ 与展示 4 的张力**是正文要点:全样本 K̂ 在 manifest,逐年 K̂ 在 Table II 与图 1/2 图例,引用数字以这些落盘产物为准。

### 12.3 调试路径建议

出问题先看 `runtime/export_summary.json` 的 failures → 对应任务的日志 tag → 若是数值异常,依次核 `submission_fast_manifest.json`(K̂/N/天数)→ diagnostics 下相应 CSV → 最后才进 builder 断点。图形异常九成在数据帧而非 matplotlib,先把 builder 的 DataFrame 打出来看。

*(完)*
