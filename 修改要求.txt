我现在希望你对项目在保证现有输出与处理功能逻辑不变的前提下实施以下修改：
1. 检查当前程序运行流程是否正常，每一步是否准确无误。
2. 彻底删除清理干净当前图表生成时没有用到的旧的冗余链路代码
3. 我现在把figcode和tablecode改成了figCode和tableCode这样的驼峰命名，注意所有的引用路径要进行相应的修改适配文件夹名变动。
4. Code文件夹下顶层代码文件只保留main.py运行函数和config.yaml配置文件，yaml要做到尽可能简洁清晰而又配置起来方便灵活，main就直接按照yaml的配置去运行相应的逻辑。然后数据相关的代码（包括build_mom_5min、getApidb、preprocess_cn_data）单独收到Code/dataPrepare/文件夹下，且前缀上step_*来提高可读性；然后export_submission_core_fast现在是不是所有表都靠这一份代码实现？请你参考Code/figCode的写法把这份代码分拆成一个表对应一份代码，替换掉现有的旧figcode里的老旧冗余函数，然后各个export_submission_table_i_fast到export_submission_table_V_fast也都对应着替换掉tablecode里的老旧表格生成函数。注意命名风格还是按照原先的figure_03、figure_03这种来命名。然后仓库根目录下“Reposit/无风险利率”这个文件夹不应该存在，用其中的risk_free.csv替换掉现在D:\Courses\机器学习\Reposit\Data\external_Data\pelger_tail\factors\rf里的shibor_对数收益率.csv，程序运行时读取\rf\里面的risk_free.csv作为无风险利率数据去进行后续的处理（不应该依赖根目录下的无风险利率文件夹），注意相应的调整代码适配表格之间的差异，注意路径匹配。
5. 恢复命令行选参数的能力，既要能够在编译器里通过修改config并运行main来达到便捷运行自己想要运行部分的效果又要能够直接在控制台去对main附加相应的参数来运行想要的部分与效果。命令行有明确附加参数时以命令行为准，没有的话像直接python main.py 就直接按照ymal配置的逻辑来。要能够控制运行【数据预处理、生成图、生成表】，同时对于这三个大类内部还要能控制下面的小项，比如能够指定生成哪张图哪张表、数据预处理具体做step几，且能够方便的去执行相应的“一键全部执行”而不必一个一个把所有的参数全写上才能完整执行。
6. 图片输出的时候不用加总标题，总标题出现在生成文件名里即可。
7. 参考《结构要求.txt》里的复刻要求。
8. 检查修改效果，是否输出准确无误、是否项目结构清晰易懂，并更新README同步至当前修改后版本，注意说明要再扩充一下尽可能详细全面完整。
