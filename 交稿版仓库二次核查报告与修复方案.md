# Pelger (2020) A股复刻 · 交稿版仓库二次核查报告与修复方案

> 核查对象:`1783061717934_Reposit.zip`(Code 41 个 .py + Result 9 图 4 表)。
> 核查方法:全部源码逐函数阅读并与论文口径比对;4 张表逐格核数;9 张图经 OCR 提取文字 + 渲染代码双向核对;对三个最易藏错的新机制(逐年 Procrustes 旋转、固定 K 广义相关、Table V 归一化反推)编写受控数值模拟验证;所有结论均锚定到 `文件:行号`。
> 本文为**二次核查**:上一轮口头结论中所有"疑似/待确认"项均已重新取证,其中 1 条判断被证伪并在 §1 正式纠正。

---

## 一、二次核实结果总表

| # | 上轮结论 | 二次核实 | 证据锚点 |
|---|---|---|---|
| 1 | 两个面板口径与用户定义严格一致 | ✅ 成立 | `step1_preprocess_panels.py:631-634`(逐年 `is_strict_{year}`);`submission_fast.py:344-368`(Fig2=full 切片);Table I 实测 N=115 恒定 / N=1319…4635 变动 |
| 2 | PCA 核心公式全部正确 | ✅ 成立 | `engine.py: pca_factors / _standardize / perturbed_eigenvalue_ratio / generalized_correlations / orient_pca_result / tangency_portfolio / time_series_pricing` 逐一复核 |
| 3 | 核心 PCA 每次运行现场重算,不再依赖旧 pickle | ✅ 成立 | `submission_fast.py:1024-1119`(`PelgerPipeline(...).run_full()` 每次执行);display 因子三段收益在 `engine.py:4643-4663` 现算 |
| 4 | 逐年对齐非平衡估计量(Procrustes q1@q2)构造正确 | ✅ 成立(模拟验证) | `paper_tail.py:1657-1773`;模拟:真同因子空间下恢复 GC=[0.999,0.998,0.995,0.990] → Panel C 实测 {0.86,0.73,0.60,0.41} 是真实经济信号 |
| 5 | Table V 从 Figure 13 归一化累计收益反推,SR 精确不变 | ✅ 成立(模拟验证) | 归一化为"同因子三段共用同一日频 std"(`paper_tail.py:1804-1815`);模拟差异 7.2e-9(求解器噪声) |
| 6 | 2019 年 GC₂≈0.008 是特征值接近时 K=2 截断的排序交换,非 bug | ✅ 成立(模拟验证) | 模拟:λ₂≈λ₃ 时 K=2 → GC₂=0.556,K=3 → 0.997 |
| 7 | **Table V 行业行是 12 资产切点,与 4 因子行不可比** | ❌ **误报,正式撤回** | `paper_tail.py:1001-1019`:`_build_industry_factor_frame` 只写 Market + selected 3 行业(共 4 因子);老 Table III `Industry full` n_factors=4 旁证;新 Table V 行业 SR(1.55)< PCA(1.60),无膨胀迹象 |
| 8 | Fig 12 的 y 轴减全额日 rf、x 轴不减 rf | ✅ 成立 | `paper_tail.py:612-614`:`mean_intraday_excess = mean(intra)`(不减),`mean_overnight_excess = mean(night − rf_daily)`(全额) |
| 9 | rf 换国债后若不刷新 paper_tail,FFC 内嵌旧 SHIBOR rf → 同表混口径 | ✅ 成立,且修复通道存在 | `paper_tail.py:1036-1038`(FFC MKT 构建时内嵌 rf);`engine.py:4674-4695`(`refresh_paper_tail=True` 会重建全部 legacy 资产层);`main.py --refresh-paper-tail` 已插线 |
| 10 | rolling 缓存兜底顺序反了(签名不匹配时优先复用无签名 CSV) | ✅ 成立 | `submission_fast.py:210-236`:`if not _rolling_signature_matches(...): cached = _load_submission_rolling_outputs(...); if cached: return cached` |
| 11 | fig1/2 诊断 CSV 无签名可能陈旧 | ✅ 成立但仅限 standalone 场景 | 主链路两个 diagnostics 目录同一(`engine.py:4378-4382` = `submission_fast.py:65-72`),`build_submission_fast_result` 每次覆盖重写 → 主链路安全;仅单独 `python figCode/figure_01.py` 时可能读旧 CSV |
| 12 | 图 12/14/15 的行业点标为 I01–I11 匿名码且无对照图例 | ✅ 成立 | `paper_tail.py:2338-2355`:`_asset_label_map` 对已知行业**刻意**返回 `I{idx:02d}`,而英文映射 `_INDUSTRY_EN_LABELS`(2323-2335)就在同文件却未被使用 |
| 13 | Fig 14/15 的 Panel A/B 标题重叠(旧版问题) | ✅ 已修复 | `paper_tail.py:2489-2495`:`fig.text` 定位 + `subplots_adjust` 预留;OCR 证实 "Panel A: Predicted Returns" 正常渲染 |
| 14 | Fig 4 图例裁切 / 中文方框(旧版问题) | ✅ 已修复 | `figCode/_weights.py:147-158`:图例内嵌 `bbox_to_anchor=(0.835,0.5)` + `tight_layout rect=(0,0,0.82,…)`;行业名走英文映射;OCR 证实 11 个英文行业名齐全 |
| 15 | 松宇宙(`valid_days>0`)与 pairwise PCA 已从交稿链路移除 | ✅ 成立 | 交稿相关模块 grep 无引用;`_panel_pca` 仅在含 NaN 时兜底 pairwise,strict 面板无 NaN 永不触发 |
| 16 | K̂ 全样本值未浮出到任何交付物 | ✅ 成立 | `submission_fast_manifest.json` 字段无 K_cont_hat;仅 `print_pipeline_summary` 打到控制台 |
| 17 | 行业因子=等权组合、FFC 的 MKT=市值加权−rf,同表两种"市场" | ✅ 成立(新发现,上轮未提) | `paper_tail.py:1009`(Market=equal_weighted)、`1012`(行业=equal_weighted) vs `1029-1030`(FFC 用 value_weighted) |
| 18 | 国债 rf 年利率分支按 /365 折日,逐交易日应用会少计约 1/3 | ✅ 成立 | `paper_tail.py:475-478`(`Nrrdata` 分支 `/365`,应用于 ~244 交易日) |

**旧版(上一份成果包)遗留问题的关闭状态**:P0-1 逐年平衡面板错列 → 已修(逐年 `is_strict_{year}`);P0-2 松宇宙 → 已移除;P0-3 pairwise PCA → 已移除出主链;P0-4 Table II 结构 → 已重写为论文式固定 K 年份列;P0-5 跨版本 checkpoint 混跑 → 架构性消除(核心现算);P1-6 rf 三套口径 → PCA 行已按 footnote 33 修正(残留见本文 P0-3);P2-13/14 字体与图例 → 已解;P2-15 Fig6 空白 / P2-16 Fig9 单线 → 图已删,问题随之消失;P2-19 表格版式 → Table I/II 已重排。

---

## 二、总体判定

**方法论层面无致命缺陷,无系统性偏误残留在"公式与估计量"这一层。** 两个面板定义与用户口径严格一致且标签诚实;PCA/跳跃识别/K̂ 判据/GC/切点组合/时间序列定价全部按论文口径实现并经复核;三个新增机制(逐年旋转、固定 K GC、Table V 反推)经受控模拟验证无构造伪影;核心因子链路每次运行现场重算,彻底消除了旧版"陈旧 pickle 混跑"这一整类风险。

剩余问题集中在四个层面,按严重度递减:**样本选择的解释责任**(N=115 的幸存者性质、K=4 展示与 K̂≈1–2 证据的张力)、**同表口径一致性**(Table V 内三组因子的 rf 与市场定义、Fig 12 双轴 rf 不对称)、**数据溯源与缓存工程**(legacy 行业 CSV 的冻结溯源、换 rf 的操作规程、两处无签名缓存)、**呈现可读性**(I01 匿名码、图题措辞)。以下逐条给出修复方案。

---

## 三、问题清单与修复方案

严重度定义 —— **P0**:影响核心数值的解释或同表可比性,交稿前必须"修复或书面声明";**P1**:稳健性缺口,强烈建议补做;**P2**:工程与呈现,低成本高收益;**P3**:非问题,是应升格为正文的发现。

### P0-1 N=115 固定交集:全仓库最大的单点解释责任

**现象**:Figure 2 面板 = 13 年零缺日的严格交集,N=115,占全市场 5465 只的 2.1%;该面板驱动 Fig 4/7/10/13/14/15 与 Table III/V 的全部 PCA 基准。

**分析**:你的定义 A("整个样本期内都连续存在、都能用")支持 strict 口径,这个选择**站得住**;但必须清醒两点。其一,论文附录 A 自己的操作化是"宽松缺失阈值 + 零增量插值"(对应仓库已有的 `paper_lenient` 面板,N=391),论文的 332/600≈55% 与这里的 115/5465≈2% 有一个数量级的选择强度差——13 年零停牌在 A 股约等于"大市值 + 高流动性 + 未卷入 2015 停牌潮"的复合筛选。其二,Table II 提供的代表性辩护只对第 1 因子完全成立(GC₁=0.90–0.99),GC₂ 在 0.007–0.90 之间波动、GC₃ 更弱,即**第 2 因子起代表性只是部分成立**;Panel C 的逐年对齐 GC={0.86,0.73,0.60,0.41} 把这一点定量化了(经模拟确认估计量本身忠实,故这组数是真实的幸存者敏感度)。

**修复方案**(三件事,一次完成):
1. **391 稳健列**:用已产出的 `paper_lenient` 面板对 Table III 与 Table V 的核心行各跑一次对照。工程现状:`load_proc_hf_panel` 已放行 `paper_lenient`(`engine.py:73` 两模式均在 `VALID_PANEL_SAMPLE_MODES`),但 `build_submission_fast_result` 在 `submission_fast.py:1035` 硬性 `raise` 非 strict。最小改法:给该函数加参数 `allow_lenient_robustness: bool = False`,或者写一个独立小脚本 `devTools/robustness_lenient.py`(加载 lenient 面板 → `PelgerPipeline.run_full()` → 复用 `refresh_replication_result_views` + `build_submission_table_iii/v` 的同一套函数),输出 `Table_III/V_robustness_lenient391.csv`。**注意**:lenient 面板含插值零增量,QV/跳跃统计有轻微下偏,此稳健列只用于 GC 与 SR,不替换 Table I。
2. **caption 声明**:Figure 2 与所有基于该面板的图表 caption 写明"固定交集为 13 年全勤股 115/5465(2.1%),隐含大市值与流动性选择;第 1 因子代表性由 Table II 支持(GC₁≥0.90),更高阶因子代表性部分成立"。
3. **行业构成表**:在 diagnostics 输出 115 只的 11 桶分布(`pd.Series([lookup[t] for t in panel.tickers]).value_counts()`,五行代码)。若某桶只剩 2–3 只,Fig 4 对应行业块的解读要在正文降权。

**验证**:391 列的 Table III GC₁₋₃ 与 115 列同向且量级接近(允许 GC₄ 有差);行业构成表无空桶。

### P0-2 "估计 K̂≈1–2,展示 K=4":三处证据互相指认,必须正面回应

**现象与证据**:(i) Fig 2 图例逐年 K̂ 多为 1–2(仅 2024:4、2025:5);(ii) Table III 中行业与 FFC 的 GC₄ 双双垫底(0.284 / 0.119)——**第 4 因子谁都张不成**;(iii) Panel C 第 4 因子跨样本构造最不稳(0.409)。而 `DISPLAY_CONTINUOUS_FACTOR_COUNT=4`(`engine.py:97`)让所有下游硬用 4 因子。同时第 4 因子又有最干净的单因子反转(盘中 −0.56 / 隔夜 +1.13),不宜直接砍。

**根因**:展示 K 沿用论文的 4;A 股证据(小 N 面板 + median_sqrtN 扰动)支持的显著因子数更少。这不是代码错误,是**证据与呈现的错位**,当前交付物里没有任何地方报告全样本 K̂,读者无从判断。

**修复方案**:
1. **浮出全样本 K̂**(两行代码):`submission_fast.py` 的 manifest 字典中加入 `"K_cont_hat": int(pipeline.K_cont_hat), "K_jump_hat": int(pipeline.K_jump_hat), "K_hf_hat": int(pipeline.K_hf_hat)`;并在 Table II 的 Panel metadata 块加一行 `K_hat (Figure 2 full-sample)`常数列。
2. **Table II 加 "First 4" 块**:`build_submission_table_ii_paper_style`(`submission_fast.py:524-525`)把 `_panel_pca(..., K=3)` 改为 `K=4`,GC 循环 `for K in (2, 3)` 改为 `(2, 3, 4)`,行定义列表补 `("First 4 continuous PCA factors", 4)` 与跳跃对应块。这直接检验"下游用的第 4 因子在两种面板间是否同一个东西"。
3. **叙事策略**:核心结论(共动结构、反转)押在前 3 因子;第 4 因子单独一段,明确表述为"边际显著、跨构造不稳、但反转特征突出的候选因子",引用 (i)(ii)(iii) 三处证据。若全样本 K̂ 实测 ≥4,则张力自然消解,只需报告该值。

**验证**:重跑后 Table II 出现 First-4 块;manifest 含三个 K̂;正文引用一致。

### P0-3 Table V 同表口径三重不一致 + 换国债 rf 的操作规程

**现象**:同一张 Table V 内,(a) PCA / 逐年对齐 PCA 行:零成本因子,不减 rf(✅ 已按 footnote 33 修正);(b) FFC 行:MKT = **市值加权**全市场 − rf,rf 按 4/24–20/24 拆分(`paper_fidelity.py:369-381`);(c) Industry 行:Market 与行业因子 = **等权**组合收益,经 `_to_excess_matrices` 减 rf,拆分为 **盘中 0 / 隔夜全额**(`paper_tail.py:1344-1351`)。于是同表存在两种"市场因子"(EW vs VW)与两种 rf 拆分(0/full vs 4/24-20/24)。

**影响量化**:rf 拆分差异 ≈ 全额与 20/24 之差 = 4/24 × 日 rf ≈ 0.10–0.13bp/日,相对行业组合隔夜收益(−2~−10bp/日)为 1–7%,**二阶,不改变任何结论方向**;EW vs VW 市场是概念差异而非误差,但读者若不知情会误比。

**修复方案**(择一,推荐 A):
- **方案 A(统一声明)**:不改数,Table V caption 写明三组的构造差异:"PCA 因子为零成本组合不减 rf;FFC 的 MKT 为 VW 市场超额(rf 按 4/24–20/24 拆分);行业组为 EW 组合超额(rf 全额计入隔夜)"。成本为零,学术上完全可接受。
- **方案 B(统一口径)**:`_segment_rf_vectors` 改为 `{"intraday": rf×4/24, "overnight": rf×20/24, "daily": rf}`(与 `split_daily_rf` 对齐),重出 Table V 行业行与 Fig 14/15 定价框架。改动 4 行,重算分钟级。

**换国债 rf 的强制规程**(必须写进 README):替换 `Data/external_Data/pelger_tail/factors/rf/risk_free.csv` 后,**第一次运行必须带 `--refresh-paper-tail`**(或 config 里 `refresh_paper_tail: true` 一次)——因为 `ffc_segmented_returns.csv` 的 MKT 在**构建时**就内嵌了 rf(`paper_tail.py:1036-1038`),不刷新则 PCA/定价用新 rf、FFC 用旧 SHIBOR rf,同表混口径。另:若国债文件走 CSMAR `Nrrdata`(年利率)分支,`paper_tail.py:478` 按 `/365` 折日、逐交易日应用会**少计约 1/3 的 rf**(244/365≈0.67);建议要么预处理成 `date,rf_log_daily` 格式直供,要么该分支改 `/annualization_days`。影响 ≈0.2–0.3bp/日,量级同上,二阶但应修。

**验证**:刷新后 diff `ffc_segmented_returns.csv` 的 MKT 列确有变化;Table V FFC 行数值随之更新;若走方案 B,行业行三段 SR 变动应 <0.05。

### P0-4 legacy 行业因子 CSV 的溯源确认(上轮误报的正确残留)

**现象**:上轮"12 资产切点"判断已撤回——`_build_industry_factor_frame`(`paper_tail.py:1001-1019`)只写 Market + 3 个 selected 行业,老 Table III `n_factors=4` 旁证,Table V 行业行确为 4 资产切点,**可比性无虞**。真正残留的是溯源问题:本次运行 `refresh_paper_tail: false`,磁盘上的 `industry_factor_returns.csv` 是**上一次**重建的产物;那次重建时 `PELGER_INDUSTRY_FROZEN` 是否已生效、还是旧占位"按集中度自动挑选"恰好选中同样 3 桶,从交付 zip 无法判定(frozen 逻辑见 `paper_tail.py:942-955`,占位兜底在其后)。

**修复方案**:查 `paper_tail/diagnostics/` 下的 industry_selection JSON(若有 `selection_rule: "EX-ANTE FROZEN..."` 即闭环);或直接带 `--refresh-paper-tail` 重建一次,diff 前后 CSV——若逐位一致则溯源闭环且顺带完成 P0-3 的 rf 刷新。**无论哪种,正文须声明"3 个行业桶为事前冻结(大金融/医药生物/周期资源),非样本内反推"——这是论文 III.D 的 ex-ante 精神,也是审稿人必问的点。**

### P1-5 K̂ 判据的规模稳健性(γ=0.08 标定于 N≈300,现用到 N=4776)

**现象**:Fig 1 的 2023–2025 年 K̂=8/7/6,ER 曲线在 1.08 临界线附近爬行(OCR 与图例证实);`g=√N·median` 与 γ=0.08 是 Pelger 在 N≈300 下模拟标定的,A 股逐年 N 达 4571–4776,q=N/M 比论文大一个量级。K̂ 逐年上行可能是真实结构(注册制后个股增多、结构更细),也可能是判据在大 N 下的漂移。

**修复方案**:`g_fn` 开关现成(`median_sqrtN / logN / none`,`engine.py:2412-2443`),挑 2015、2020、2023 三个代表年,对 Fig 1 的面板各跑三种 g_fn 的 K̂ 对照,汇成一张 3×3 小表放附录。成本:每年一次特征分解已缓存,只需重算 ER,分钟级。**判读标准**:若三种 g_fn 下 2023 年 K̂ 均 ≥6,则"因子数上行"可作为中国发现写;若仅 median_sqrtN 给出高值,则正文对 2023–25 的 K̂ 加"判据敏感"标注。

### P1-6 涨跌停触板诊断(解释 Table I 跳跃占比偏高的钥匙)

**现象**:a=3 下 `frac_jump_increments`≈1.4%(论文 0.53–0.89%)。A 股 10%/20%/30% 涨跌停会把连续动态截断成"贴板 + 大增量",天然被阈值法标为跳跃;这不是 bug,但不解释就是审稿破绽。

**修复方案**:在 `detect_jumps` 的 stats_out 或独立诊断中加一列——被标为跳跃的 bar 中,收盘价触及当日涨跌停价的占比(需要 bar 级 high/low 与前收计算限价,预处理阶段已有原始 K 线;若不便回溯原始价,可用 `|5min 收益| ≥ log(1.095)` 的近似代理并声明)。Table I caption 补一句"跳跃占比高于美股,其中 X% 与涨跌停触板相关"。

### P1-7 2015 年 N=557 的年内平衡塌缩

**现象**:停牌潮使 2015 年"年内全勤"股仅 557 只——**市场最需要因子结构分析的年份,恰是面板选择最强的年份**。2015 年 Table I 的解释度尖峰(44–46%)与 Fig 1 的 K̂=2 都建立在这个幸存子集上。

**修复方案**:声明为主(Fig 1 与 Table I caption 各一句);可选增强:对 2015 单年用 `paper_lenient` 年面板(覆盖率≥96% + 零插值)复算 K̂ 与解释度作脚注对照。

### P1-8 切点 SR 的样本内属性与 A 股可实施性声明 + 两行改名

**现象**:Table V 全部 factor_set_tangency 为全样本 in-sample 最大夏普(`tangency_portfolio` 的 √(μ′Σ⁻¹μ) 恒非负);隔夜 2.28–2.85 的本质是"做空市场隔夜"(F1 隔夜 SR=−2.27、MKT=−1.90)。在 T+1(禁止当日买入当日卖出)与融券约束下,该策略**不可按面值实施**;Lou–Polk–Skouras 的机构/散户下单机制在 T+1 下也需重新表述。另:"PCA overnight"/"PCA daily" 两行名称有歧义(实为"用某段权重跨段评估")。

**修复方案**:caption 三句话(in-sample;本质 short-market-overnight;T+1/融券不可实施,数值为收益分解而非可交易套利);两行改名为 `PCA (overnight-tangency weights)` / `PCA (daily-tangency weights)`。同时把这两行**升格为正文证据**——同一组权重跨段变号(隔夜权重放盘中 SR=−1.34,日频权重放隔夜 SR=−1.86)是比散点图更干净的反转证明。

### P2-9 rolling 缓存兜底顺序反了(潜在陈旧,当前未发作)

**现象**:`submission_fast.py:210-236` 的 `_load_or_build_rolling_outputs`——签名不匹配或 checkpoint 缺失时,**先**尝试无签名的 diagnostics CSV 缓存,命中即返回;只有 CSV 也缺才重算。含义:改 γ/g_fn/面板后重跑,Fig 7/10 的回退路径可能静默复用旧参数的滚动结果。当前运行 Fig 7/10 主路径走 `_timevar` 现场局部 PCA(`figCode/_timevar.py:64-130`),未受影响;这是回退路径上的地雷。

**修复方案**:两处任选——(a) 交换顺序:签名不匹配 → 直接重算,CSV 缓存仅在"签名匹配但 checkpoint 缺失"时使用;(b) `_persist_rolling_outputs` 时旁存一份 `rolling_signature.json`,`_load_submission_rolling_outputs` 加载前比对。改动 <15 行。

### P2-10 fig1/2 诊断 CSV 的 standalone 陈旧风险

**现象**:主链路安全(两个 diagnostics 目录同一,每次 `build_submission_fast_result` 覆盖重写);但单独运行 `python figCode/figure_01.py` 时,`load_or_build_submission_figure_factor_counts`(`engine.py:4453-4468`)只要 CSV 存在就直接读,不校验参数;且 `run_standalone` 的 `get_result(allow_build=False, allow_fallback=True)` 可能绑定回退 pickle。

**修复方案**:诊断 CSV 里已有 `g_fn/gamma` 列——加载后与当前 cfg 比对,不一致则重建(6 行);或在 README 声明"单跑图任务仅用于调试,交稿产物一律走 `python Code/main.py`"(零成本)。

### P2-11 Fig 12/14/15 的 I01–I11 匿名码:映射已存在却被绕开

**现象**:`_asset_label_map`(`paper_tail.py:2338-2355`)对已知行业刻意返回 `I{idx:02d}`,而 `_INDUSTRY_EN_LABELS`(同文件 2323-2335,11 桶英文名齐全)未被使用——读者无法知道 Fig 12 中间面板、Fig 14 Panel B 横轴的点/柱对应哪个行业。Fig 4 用的是另一函数(`figCode/_weights.py:79-88`)走了英文名,所以只有 paper_tail 系三张图受影响。

**修复方案**(一行):`_asset_label_map` 中 `elif raw in _INDUSTRY_EN_LABELS: mapping[raw] = f"I{idx:02d}"` 改为 `mapping[raw] = _INDUSTRY_EN_LABELS[raw]`。若担心长名拥挤,Fig 12 保留短码但在图右侧加 code→name 图例(与 Fig 4 同款 Patch 图例,~10 行)。

### P2-12 Fig 12 双轴 rf 不对称

**现象**:x=原始盘中均值(rf=0),y=隔夜均值−全额日 rf(`paper_tail.py:612-614`)。行业/2×3 面板 y 显示量级 −2~−10bp,rf≈0.6–0.8bp/日的整体下移占顶端点位 7–30%;反转斜率不受影响,但"所有行业隔夜为负"的水平读数被略微夸大。

**修复方案**(择一):(a) 最干净——两轴均用**原始收益**,轴标签去掉 "excess",反转叙事不需要超额口径;(b) 两轴按 4/24–20/24 拆分 rf。改 `_build_figure12_*` 两行 + 重出图(需 `--refresh-paper-tail` 或在 submission_fast 侧重算该帧)。caption 同步。

### P2-13 呈现与工程杂项(合并处理)

1. **图题措辞**:`io_utils.py FIGURE_SPECS` 中 Fig 1/2 仍叫 "Unbalanced Panel"/"Balanced Panel",与新语义不符;建议改为 "Yearwise-Balanced Changing Universe" / "Fixed-Intersection Balanced Panel"(文件名可保留旧名以稳定链接,题注必须改)。
2. **Fig 13 图例** `MKT_excess` 内部名外漏:渲染层加 `{"MKT_excess": "MKT"}` 显示映射。
3. **陈旧报错文案**:`engine.py:1501/1611` 的 "当前数据政策仅保留 strict_balanced 面板" 与实际(两模式均放行)矛盾,改为通用 Unknown sample_mode 文案。
4. **Table I 加 λ₁ 占比行**:逐年第一特征值 / trace,把"A 股市场因子支配性(ER₁ 大)"从图上的视觉现象变成可引用的数(每年一次除法,数据已有)。
5. **Table II 加 Figure 2 面板的 K̂ 行**:与现有 `K_hat (Figure 1 HF)` 对称。
6. **`.gitignore` 忽略了 `Result/`**:交付 zip 是手动打包的;若课程要求 git 提交结果,需移除该行或改交付说明。
7. **Table V 的 SR_daily 列理解提示**:PCA daily 切点 0.47 远低于盘中 1.60 与隔夜 2.52——这本身是反转的推论(盘中/隔夜溢价对冲),caption 一句话点破,防止被读成"模型日频表现差"。

### P3 应升格为正文的发现(非问题)

1. **FFC(尤其 SMB)张成 PCA 空间的能力不弱于行业因子**:Table III 实测 FFC GC={0.967, **0.929**, 0.692, 0.119} vs Selected industry GC={0.976, **0.766**, 0.660, 0.284}——第 2 广义相关上 FFC 显著胜出,与原文"行业 ≫ 特征因子"的头号结论**方向相反**。建议补一个显式消融:Market+SMB、Market+SMB+周期资源 两组 GC(在 `build_submission_table_iii` 加两行,ffc_segmented 里 SMB 现成),把"SMB 在 A 股本身就是协动因子"钉死为本复刻最有分量的中国发现。
2. **规模溢价在盘中**:Size 盘中 +0.93 / 隔夜 −2.32,与美股 SMB 溢价集中在隔夜正好相反;Value 隔夜 +1.17 与美股同向。干净的跨市场对照,直接写进 Table V 的讨论段。
3. **危机年解释度尖峰**:2015(44–46%)与 2024(33–36%)是论文 2008 叙事的 A 股对应,Table I 已天然呈现,正文点题即可(2015 需带 P1-7 的声明)。
4. **幸存者敏感度的定量化**:Panel C 的 {0.86, 0.73, 0.60, 0.41} 经模拟排除构造伪影后,可作为"平衡面板结论对样本构造的稳健程度"的量化指标写入正文——这比论文只做定性讨论更进一步。

---

## 四、修复执行顺序(最小重算路径)

全部修复不需要任何小时级重跑;逐年面板 PCA 诊断已缓存,核心管线本就每次分钟级现算。

**第一批(一次运行解决,合并改码后跑一次 `python Code/main.py --refresh-paper-tail`)**:
P0-3 rf 拆分统一或声明 + P0-4 行业溯源(刷新即闭环)+ P2-11 行业标签 + P2-12 Fig12 双轴 + P2-13 各杂项 + P0-2 的 manifest/K̂ 浮出与 Table II First-4 块 + P1-8 改名。刷新会重建全部 legacy 资产层,顺带把行业/FFC/2×3/figure12 数据统一到当前代码与当前 rf。

**第二批(独立小脚本,分钟级)**:
P0-1 的 391 稳健列(`devTools/robustness_lenient.py`)+ 115 只行业构成表;P1-5 的 g_fn 3×3 对照;P1-6 触板诊断列。

**第三批(纯文字)**:
所有 caption 声明(N=115 选择性质、in-sample/T+1、EW vs VW 市场、2015 塌缩、跳跃占比与触板、第 4 因子定位)+ README 补"换 rf 必须 --refresh-paper-tail"规程。

**第四批(替换国债 rf 时)**:
放入新 `risk_free.csv`(推荐直接给 `date,rf_log_daily` 格式,绕开 /365 分支)→ `python Code/main.py --refresh-paper-tail` → 核对 `export_summary.json failures=[]` → diff FFC 行变化。

## 五、修复后复核清单

1. `Table_II` 出现 First-4 块与两面板 K̂ 行;manifest 含 `K_cont_hat`,正文引用一致。
2. `Table_III/V_robustness_lenient391.csv` 与 115 主列 GC₁₋₃/SR 同向、量级接近;GC₄ 差异如实报告。
3. 刷新后 `industry_selection` 诊断显示 `EX-ANTE FROZEN`;`ffc_segmented_returns.csv` 的 MKT 列随新 rf 变化。
4. Fig 12/14/15 行业标签可读(英文名或带图例的短码);Fig 1/2 图题与新语义一致。
5. g_fn 3×3 对照表:2023 年 K̂ 在三种 g_fn 下的稳定性有明确判读写入附录。
6. 触板诊断列产出,Table I caption 引用其数值。
7. `python Code/devTools/check_submission_outputs.py` 全 OK,`export_summary.json` failures 为空。
8. 消融行(Market+SMB 等)落入 Table III,正文以其为轴心组织"中国修正"一节。

---

*附:本次核查中被证伪并撤回的判断——"Table V 行业切点为 12 资产"(§1 第 7 条)。其余上轮结论全部经二次取证成立。*
