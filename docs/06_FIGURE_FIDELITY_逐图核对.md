# 逐图 1:1 复刻核对（FIGURE FIDELITY）—— 对照论文 PDF 实图

> 本版基于**打开论文 PDF 逐张渲染实图**核验。此轮把【图 1–15 全部】的生成代码
> 改到尽量贴合论文形态——包括之前缺的时间变化类（6/7/8/9/10/11）。
> 缺失数据（如 R_cont、非平衡逐年 PCA 等）由你后续补充并运行；代码按
> “完整数据可用”写好，并对每张图加 try/except：**论文形态优先，失败回退旧绘图**。
> 重要：**所有代码未经运行验证**（lite 沙箱无数据），请在本机出图核对。

## 出图路径
- **figcode/registry 路径（`main.py` task runner）= 本文所有修复落地处。出图请走它。**
- engine `export_all_paper_figures` all-in-one 路径：只共享 Fig 1/2 判别线；3/4/5 仍简化热图、6–11 仍旧折线（拿不到行业映射/自洽滚动）。

## 自洽滚动计算（新增 figcode/_timevar.py）
图 6/7/8/9/10/11 不再依赖既有缓存里存了什么，而是从 `pipeline.R_cont`（连续 5min）
+ `panel.day_ids`（5min→交易日映射）+ `panel.R_daily` **重算 21 日局部窗口**得到论文所需全部量
（局部 PCA 载荷/权重/因子方差/解释度；行业·FFC 用局部回归载荷）。

---

## 逐图状态

| Fig | 论文形态（实图核验） | 代码现状 | 备注 |
|----|----------------------|----------|------|
| 1/2 | 4 子图(年份组) ER + 绿色 Critical value 线 | ✅ 子图分组 + 判别线 | CN k_max=10、2013–2025 |
| 3 | 每因子1子图，行业着色**柱状**，x=股票按行业，y=Loadings | ✅ 柱状(全市场 proxy 权重) | A 股 11 桶色例 |
| 4 | 同上(连续 PCA，Factor 1–4) | ✅ 柱状(全市场连续权重,含 P4 符号) | |
| 5 | 同上(月频 PCA) | ✅ 柱状(全市场月频载荷,含 N12 符号) | |
| 6 | **6 面板**(连续4/HF4/行业4/FFC4/FF3/市场1) 载荷 GC 时序 | ✅ 6 面板(连续·HF 用局部 PCA；行业·FFC·FF3·市场用局部回归载荷) | 需 R_cont + 各因子日频收益 |
| 7 | 单面板，前 **7** 因子**权重** GC 时序 | ✅ 7 线权重 GC(局部 PCA K=7 vs 全样本) | 需 R_cont |
| 8 | **4 因子 × 2 月份窗口** 权重行业着色柱状 | ✅ 4×2 柱状(默认取低/高波动两窗口；可传两日期) | 需 R_cont |
| 9 | 解释方差时序 | ✅ 局部 PCA 解释度时序 | |
| 10 | 连续 PCA：[系统性影响/平均载荷/波动]×[原始/归一化]，3×2，4 线 | ✅ 局部 PCA 三量分解 3×2 | 需 R_cont |
| 11 | 同 10，但 4 个 **FFC** 因子(载荷=局部回归) | ✅ 局部回归载荷 + FFC 因子方差，3×2 | 需分段 FFC 日频因子 |
| 12 | **3 面板**(All/Industry/Sorted)，空心圆，向下趋势虚线，x=盘中 y=隔夜 | ✅ 3 面板 + 空心圆 + 趋势线 | |
| 13 | 行=因子集(PCA/PCA-unbalanced/FFC)，列=频段，4 线 | ✅ 三行齐(unbalanced=全市场 pairwise PCA 近似) | 近似口径,见 07 |
| 14/15 | Panel A(2×3 散点+45°线) + Panel B(2×3 定价误差柱状) | ✅ 已拆 A/B，含 Panel 标题 | |

✅=已改到论文形态（待你补数据运行核验）；◑=朝向已对、仍缺一行数据。

---

## 你补数据后需要满足的输入（让论文形态生效，否则自动回退）
- Fig 6/7/8/9/10：`result.pipeline.R_cont`（连续 5min，(M_total,N)）+ `panel.day_ids` + `panel.R_daily`。
- Fig 6（行业/FFC/FF3/市场面板）、Fig 11：`result.paper_tail` 里的 `industry_factors` 与（修好的）`ffc_segmented` 日频因子。
- Fig 8 想指定月份：调用处把两个日期传给 `render_fig8(..., windows=["2018-10","2020-02"])`（默认自动取低/高波动两窗口）。
- Fig 13 的 “PCA-unbalanced” 行：需要非平衡面板逐年 PCA 的因子收益（完整路径产物）；缺失则该行留空。

## 表（Table）
- **Table III**（N9）：已补 HF PCA / PCA Proxy 行 + 下半部 ω 频率载荷 GC，新增 `panel` 列。
- **Table V**（N10）：已补 Market/Size/Value/Momentum 个体夏普；年化可配置。
- **Table I/II/IV**：来自重 yearly 分析缓存（你重跑产出），其逐年(K̂、N、连续/跳跃 3&4 因子 GC)与逐频段分解结构本就镜像论文，未盲改重算逻辑。

> 仍是诚实边界：未运行验证；出图走 figcode/registry 路径；engine all-in-one 路径不含这些图的论文形态。
